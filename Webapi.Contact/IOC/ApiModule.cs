using Autofac;
using Module = Autofac.Module;

namespace Webapi.Contact.IOC
{
    public class ApiModule:Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            //base.Load(builder);
        }
    }
}
